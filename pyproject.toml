[project]
name = "mcstat"
version = "1.1"
description = "Simple tool to get minecraft server info"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.2.0",
    "pyinstaller>=6.20.0",
    "requests>=2.33.1",
]

[project.scripts]
mcstat = "src.main:main"
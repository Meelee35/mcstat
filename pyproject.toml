[project]
name = "mcstat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Meelee35", email = "124620845+Meelee35@users.noreply.github.com" }
]
requires-python = ">=3.14"
dependencies = [
    "pillow>=12.2.0",
    "requests>=2.33.1",
]

[project.scripts]
mcstat = "mcstat.main:main"

[build-system]
requires = ["uv_build>=0.11.6,<0.12.0"]
build-backend = "uv_build"

